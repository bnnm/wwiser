<div class="list closable ${extra}" data-id="${id}">
    <div class="head">
        <span class="attr type">list</span>
        <span class="attr name">${attrs['name']}</span>
        <span class="attr value">${attrs['count']}</span>
    </div>
    <div class="body">
        ${body}
    </div>
</div>
