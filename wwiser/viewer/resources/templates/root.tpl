<div class="root closable ${extra}" data-id="${id}">
    <div class="head">
        <span class="attr type">bank</span>
        <span class="attr name">v${attrs['version']}</span>
        <span class="attr value">${attrs['filename']}</span>
    </div>
    <div class="body">
        ${body}
    </div>
</div>
